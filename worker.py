class Worker:
    def __init__(self, name, worker_id):
        self.name = name
        self.worker_id = worker_id
        #ако билетът е взет online, може би default_name и default_id??

    def add_passenger():
        #да се прави връзка с втората база - add_entry
        #redirect(url_for('add_entry'))
